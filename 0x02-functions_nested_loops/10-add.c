#include "main.h"
/**
*add - a function that adds two integers
*@one: first integer
*@two: second integer
*Return: first integer + second integer
*/

int add(int one, int two)
{
	return (one + two);
}
