#include "main.h"
#include <stdio.h>

/**
*_puts - a function that prints a string until the first newline character
*@str: the pointer
*Return: nothing
*/

void _puts(char *str)
{
	while i = 0;
	while (*str[i] != '\n' && s[i] != '\0')
	{
		putchar(s[i]);
		i++;
	}
}
